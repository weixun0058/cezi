name: zhugeshensuan

services:
  app:
    image: "${IMAGE_NAME:-zhugeshensuan}:${IMAGE_TAG:-4.1}"
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      args:
        APP_VERSION: "${IMAGE_TAG:-4.1}"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      APP_PORT: "8000"
      SITE_BASE_URL: "${SITE_BASE_URL:?SITE_BASE_URL is required}"
      CONTACT_EMAIL: "${CONTACT_EMAIL:?CONTACT_EMAIL is required}"
      ARTICLE_ADMIN_PATH: "${ARTICLE_ADMIN_PATH:-165131}"
      ARTICLE_ADMIN_PASSWORD: "${ARTICLE_ADMIN_PASSWORD:-}"
      TRUSTED_PROXY_HOPS: "${TRUSTED_PROXY_HOPS:-1}"
      SECRET_KEY: "${SECRET_KEY:?SECRET_KEY is required}"
      AI_API_KEY: "${AI_API_KEY:?AI_API_KEY is required}"
      AI_BASE_URL: "${AI_BASE_URL:-https://api.deepseek.com}"
      AI_MODEL: "${AI_MODEL:-deepseek-v4-flash}"
      AI_GLOBAL_DAILY_LIMIT: "${AI_GLOBAL_DAILY_LIMIT:?AI_GLOBAL_DAILY_LIMIT is required}"
      REFERENCE_DB_PATH: /app/data/reference/reference.db
      RUNTIME_DB_PATH: /app/instance/runtime.db
      ARTICLES_PATH: /app/content/articles_en
    ports:
      - "127.0.0.1:${APP_PORT:-8000}:8000"
    volumes:
      - runtime-data:/app/instance
      - article-content:/app/content
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    stop_grace_period: 35s
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/readyz', timeout=3)"
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  runtime-data:
  article-content:
