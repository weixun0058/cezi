# 2026-07-18 首次真实生产备份验收记录

- **执行日期：** 2026-07-18
- **快照日期：** 2026-07-18（精确 UTC 时间保留在服务器备份文件中）
- **执行环境：** `getwiseoracle.com` 生产服务器
- **结论：** runtime 数据库与文章卷备份成功，服务恢复并通过本机/公网就绪检查

## 1. 操作边界

执行前核对 Compose app 容器及 runtime、文章两个生产卷的挂载点，只有卷名与部署配置
精确匹配后才继续。

备份期间短暂停止 app 容器，未执行 `docker compose down -v`，未删除、替换或恢复生产卷，
未读取或输出 `.env`、Cookie、API key 或用户内容。

## 2. 生产版本与公开边界

执行时已记录部署镜像标签和不可变 image ID，并核对 runtime、文章两个生产卷。
由于本仓库公开，image ID、生产卷名、精确文件名及完整校验哈希只保留在服务器侧，
不写入公开验收文档。

## 3. 有效备份

| 备份 | 服务器侧证据 | 公开结论 |
| --- | --- | --- |
| runtime SQLite | 时间戳文件、字节数及独立 `.sha256` | 已生成且校验通过 |
| 文章卷 | 时间戳归档、字节数及独立 `.sha256` | 已生成且校验通过 |

两个备份及其 `.sha256` 文件保存在服务器受限备份目录；目录权限为 `700`，备份和哈希
文件权限为 `600`。

## 4. 完整性与恢复服务

- runtime：宿主机 Python 以 SQLite 只读 URI 执行 `PRAGMA quick_check`，结果为 `ok`。
- runtime `.sha256`：`sha256sum --check` 通过。
- 文章 `.sha256`：`sha256sum --check` 通过。
- 文章归档：`tar -tzf` 通过，归档包含 3 个条目。
- app 容器恢复后状态：`healthy`。
- 本机 `/healthz`：通过。
- 本机 `/readyz`：通过。
- 公网 `https://getwiseoracle.com/readyz`：通过。

## 5. 首次失败尝试

第一次尝试已复制 runtime 文件，但应用镜像内 Python 因宿主 bind mount 权限无法打开该文件，
因此没有生成 SHA-256，也没有被认定为有效备份。退出保护成功恢复应用，随后三个健康检查均
通过。该文件未删除，而是移动到受限备份目录的 `failed/` 子目录并标记为
`failed-incomplete`，不会与有效备份混用。

第二次使用服务器宿主机 Python 与经过名称核对的 Docker volume mountpoint 完成备份，避免
重复失败路径。

## 6. 限制与下一步

本次证明生产服务器本地备份、哈希和内容完整性成立；尚未把副本复制到加密的异机存储，
也没有在生产服务器上执行恢复。恢复机制已在 OPS-002 隔离卷演练中验证。按照 runbook，
后续应至少每日备份，并把有效备份复制一份到加密的异机存储，以满足“服务器损坏时仍可用”
的备份目标。
