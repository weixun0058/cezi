# SAFE-003 / SAFE-004 批次验收记录

> 日期：2026-07-15
> 状态：已完成
> 最终裁决人：项目所有者
> 实施依据：`docs/reviews/english-content-owner-review/`

## 1. 完成范围

- 74 个签文问题字段已全部按项目所有者批准的“改后字段”写回 `data/content/oracle_signs_en.json`，覆盖 69 签。
- 写回前逐项验证正文等于审查册“当前完整字段”；74/74 项一致，没有覆盖审查后发生的其他正文修改。
- 实施前修正项目所有者批复稿中 5 处仍残留的治疗/投资操作语义，并修正第 94 签一处英文语病；修正后的完整字段同步保存在审查册和正文中。
- 黄历 `medical_care` 分类从人工审校源和生成后的英文词表中删除；公开活动类别由 39 个变为 38 个。
- 黄历 `legal_matters` 分类保留。API 在结果出现该分类时返回 `safety_notices`，日结果页和十日结果卡显示“文化参考、不是法律意见或案件结果预测、具体决定咨询合格法律专业人士”的提示。

## 2. 五处残留修正

| 位置 | 最终处理 |
| --- | --- |
| 第 299 签 `health` | 删除“不得中断、否则前功尽弃、初效复发”等疗程语义，改为一般性的耐心和自我照护提醒 |
| 第 264 签 `wealth` | 删除隐含退出时机的 `do not linger`，改为收益期不宜过度自信的一般趋势表达 |
| 第 290 签 `wealth` | 删除 `co-invest with trustworthy partners`，仅保留可靠合作和储蓄导向 |
| 第 334 签 `wealth` | 删除“长期房地产有利”，改为土意象对应务实、积累和长期稳定 |
| 第 356 签 `wealth` | 删除 `taking profits when they appear`，改为克制贪念、保持耐心 |

第 94 签 `wealth` 的 `suggests—do not chase` 同时修正为语法完整的 `suggests patience; do not chase`。

## 3. 验收证据

- 批复映射：74/74 个字段等于审查册最终“改后字段”；69 个签号；正文仍为 384 签。
- 黄历词表：38 个活动类别；`medical_care` 不存在；`legal_matters` 存在。
- 真实 API：`2026-01-19` 返回 `legal_matters`，同时返回完整法律事项提示。
- 页面验收：1440×900 与 390×844 均实际渲染法律提示；无横向溢出，提示完整可见，浏览器控制台无 warning/error。
- `pytest -W error::ResourceWarning -q`：通过。
- `black --check app.py zhugeshensuan deploy/gunicorn.conf.py scripts tests`：通过。
- `ruff check app.py zhugeshensuan deploy/gunicorn.conf.py scripts tests`：通过。
- `pip-audit -r requirements.txt --no-deps --progress-spinner off`：无已知漏洞。
- 六个关键 JavaScript 文件 `node --check`：通过。
- reference DB 从权威源重建成功，logical dump 与受控数据库一致：`hanzi=18751`、`gua=384`、`pzbj=120`、`gua_hant=384`、`pzbj_hant=120`。
- `git diff --check`：通过。

## 4. 环境限制

本轮无法重新执行 Docker 生产镜像构建与容器健康探针：本机 Docker Desktop 后台服务未运行，当前会话无管理员权限启动 `com.docker.service`。这不影响 SAFE-003/004 内容实施和本地运行验收；Docker 构建与 `/healthz`、`/readyz` 容器探针须在 Docker 服务可用时补跑，并仍是 OPS-005 上线前验收的一部分。

## 5. 结论

项目所有者批准的 74 个签文字段和 2 个非签文决定均已进入真实正文或运行路径。SAFE-003 与 SAFE-004 完成，不再保留待批复内容；后续正文变更继续遵循 SAFE-001 的“行业中间偏稳健”边界。
