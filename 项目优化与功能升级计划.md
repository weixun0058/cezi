# 诸葛神算 V3 综合优化计划

## 执行信息

- 分支：`codex/project-modernization`
- 基线提交：`ad04bfa` (`v4.0`)
- 状态：代码实施与本地验收完成
- 工作区保护：未覆盖、回滚或提交执行前已有的 `database/huangli.db`、`CODEBUDDY.md` 和未跟踪缓存文件。
- 外部事项：项目维护者仍须在 DeepSeek、百炼和 SiliconFlow 控制台撤销源码历史中暴露的旧 Key，并把新 Key 作为 `AI_API_KEY` 注入部署环境。

## 阶段一：安全与运行基础

- [x] 移除源码中的模型密钥，改为环境变量配置。
- [x] 增加 `.env.example`，补全并固定 Python 依赖。
- [x] 增加 `.gitignore`，停止跟踪缓存、日志、环境文件和运行时数据库。
- [x] 备份当前黄历数据库，将运行时数据库迁移至 `instance/`。
- [x] 生产环境关闭 debug，增加请求大小、超时和结构化日志配置。

## 阶段二：后端、数据层与算法

- [x] 修复 `/api/lunming/analyze` generator 使用错误。
- [x] 新增 `POST /api/lunming/stream`，旧 GET 接口保留一个弃用周期。
- [x] 统一 API 成功和错误结构，并保持现有路径兼容。
- [x] 修复黄历缓存，使用算法版本控制缓存失效。
- [x] 将卦象 Excel 数据构建为签号索引并返回命名字段。
- [x] 为汉字笔画网络回退增加超时、状态检查和失败缓存。
- [x] 实现 `/api/pzbj_explanation`。
- [x] 使用 application factory、Blueprint 和服务层拆分模块。
- [x] 使用确定性算法计算四柱基础数据，再交给 LLM 解读。
- [x] 支持时辰未知、时区和可选真太阳时修正。
- [x] 清理不负责任的提示词和确定性高风险结论。

## 阶段三：用户功能与体验

- [x] 黄历增加结婚、搬家、开业、出行、签约、理发场景筛选。
- [x] 九天黄历支持按场景比较并进入日期详情。
- [x] 算事增加问题摘要、确认和重复起卦提醒。
- [x] 使用本地存储保存收藏、算事记录和论命报告，并支持删除、清空和导出。
- [x] 按用户反馈移除简版/详版、复制、打印和分享操作，减少低价值控件。
- [x] 区分所选日期时辰和当前实时钟表。
- [x] 统一加载、空数据、失败、超时、中断和重试状态，移除生产页面阻塞式 `alert`。
- [x] 增加娱乐参考与非医疗、投资、人生决策建议声明。
- [x] 验证移动端、键盘操作、表单标签和颜色对比度。

## 阶段四：工程化、部署与文档

- [x] 增加 Dockerfile、Gunicorn 配置和健康检查。
- [x] 增加 Ruff/Black、前端静态检查、自动化测试和 CI。
- [x] SSE 改为模型文本块并通过动画帧批量更新 DOM。
- [x] 使用标准 logging，禁止记录个人信息和完整模型输入输出。
- [x] 修订 README、API 文档和部署指南。
- [x] 记录 `日历/` 实验目录与生产代码边界，不擅自删除历史资料。

## 验收清单

- [x] Python 语法与 Black/Ruff 检查通过。
- [x] 17 项单元和 API 测试通过。
- [x] 仓库扫描不再发现有效格式的硬编码密钥。
- [x] 个人出生信息不再出现在新流式接口 URL 和日志中。
- [x] 黄历缓存命中不会重复计算或持续修改受 Git 跟踪的数据库。
- [x] SSE 不逐字符发送，浏览器不逐字符触发 DOM 更新。
- [x] 生产配置启动和 `/healthz` 健康检查通过。
- [x] 桌面端和 390×844 移动视口核心流程完成浏览器验证。

## 验证记录与遗留问题

- Black、Ruff、pytest 和三个生产 JavaScript 文件的 `node --check` 均通过。
- 浏览器验证覆盖黄历场景筛选、算事问题确认、论命 POST 流、无 Key 错误、桌面布局和移动端横向溢出。
- 移动端初测发现 8px 横向溢出，已通过全局盒模型和容器宽度修复，复测三页均无横向溢出。
- 本机未配置新模型密钥，因此只验证了确定性命盘、SSE 协议和缺配置错误；真实供应商输出需在部署环境使用新 Key 冒烟测试。
- 浏览器截图接口超时，已用 DOM、控制台、交互状态和布局几何完成验收，没有生成截图附件。
- Dockerfile 已静态检查但本机未执行镜像构建；CI 会在后续提交时执行 Python 和前端检查。
